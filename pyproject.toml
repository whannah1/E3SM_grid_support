[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "taos"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["taos*"]

[tool.setuptools.package-data]
taos = ["*.yaml"]
