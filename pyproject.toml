[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "swag"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["swag*"]
